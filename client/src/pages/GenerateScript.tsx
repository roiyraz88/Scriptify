import { Typography, Box } from "@mui/material";

function GenerateScript() {
  return (
    <Box p={4}>
      <Typography variant="h3">🛠️ Generate Script Page</Typography>
      <Typography variant="body1" mt={2}>
        Here you will build your custom Python script...
      </Typography>
    </Box>
  );
}

export default GenerateScript;
